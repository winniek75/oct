# OCTO BICYCLE RENTAL サイト設置手順書（Xサーバー用）

## 1. アップロード（10分）

1. Xサーバーの「ファイルマネージャ」または FTP（FileZilla等）にログイン
2. `octobicycle.com/public_html/` の中に `rental` フォルダを作成
3. このフォルダ一式（index.html / css / js / images / robots.txt / sitemap.xml / .htaccess）をすべてアップロード
4. `https://octobicycle.com/rental/` にアクセスして表示を確認

※ トップページ（ドメイン直下）として使う場合は `public_html/` 直下に置き、
   `.htaccess` 内と `index.html` 内・`sitemap.xml` 内の「/rental/」をすべて「/」に置換してください。

## 2. 最初に書き換える箇所（15分）

| ファイル | 箇所 |
|---|---|
| `js/config.js` | 料金・車種名・問い合わせメールアドレス（★このファイルが管理の中心です） |
| `index.html` | JSON-LD内の住所・電話番号・緯度経度・営業時間 ／ Googleマップ埋め込みURL ／ LINEのURL |
| `js/i18n.js` | `co.addrval`（住所）`co.hoursval`（営業時間）の4言語分 |
| `images/` | README.txt の通りに写真を配置（octobicycle.comのWordPressメディアからDL） |

## 3. 決済（Stripe）の設定（30分・コード編集ほぼ不要）

1. https://stripe.com/jp でアカウント作成（無料・月額0円）
2. ダッシュボード →「商品」→ 車種×時間ごとに商品を作成（例：eバイク 1日 ¥5,000）
3. 各商品で「支払いリンクを作成」→「数量の変更を顧客に許可」にチェック
4. 発行されたURL（buy.stripe.com/…）を `js/config.js` の `paymentLinks` に貼り付け
5. Stripe設定で Apple Pay / Google Pay / 中国系カード(UnionPay) をオンにする

→ リンク未設定の間は、予約ボタンは自動で「メール仮予約」として動くので、
   サイト公開を先行できます。

※予約の空き管理まで自動化したくなったら：STORES予約 / RESERVA（無料プランあり・多言語対応）の
  予約ページを作り、`js/main.js` の決済ボタンの遷移先をそのURLに差し替える運用も可能です。

## 4. SEO / MEO チェックリスト（公開後1週間以内）

- [ ] **Googleビジネスプロフィール**（MEOの本丸・無料）に登録
      https://business.google.com → カテゴリ「レンタサイクル」「自転車店」、写真10枚以上、
      4言語の説明文、サイトURLを登録。口コミ依頼カードを店頭に置く
- [ ] **Googleサーチコンソール**に登録し `sitemap.xml` を送信
- [ ] Bing Webマスターツールにも同様に登録（訪日客はBing/Naver経由も多い）
- [ ] TripAdvisor / Google Maps / じゃらん遊び体験 / Klook・KKday（韓国・中華圏に強い）への掲載を検討
- [ ] 店頭にGoogleマップ口コミQRコードを設置（英語で「Review us!」）

### サイトに実装済みのSEO/MEO施策
- hreflang 4言語（日英韓中）＋ canonical
- 構造化データ：BikeStore（店舗情報・価格・営業時間・座標）／ FAQPage
- OGP / Twitterカード、sitemap.xml、robots.txt
- .htaccess による HTTPS統一・gzip圧縮・ブラウザキャッシュ（表示速度＝SEO要因）
- 画像 lazy-load（マップ）、フォント preconnect

## 5. 運用のコツ

- 写真は「外国人が乗っている写真」「店主の顔が見える写真」があると予約率が大きく上がります
- 料金改定は `js/config.js` の数字を変えるだけ（Stripe側の商品価格も忘れずに変更）
- 英語ページのURLを配布する時は `https://octobicycle.com/rental/?lang=en` を使うとその言語で開きます

---

## 改良版で追加された機能（2026-08 更新）

参考サイト（tokyo-rental-bicycle.com）の「外国人観光客に効く要素」を取り入れました。
**設定はすべて `js/config.js` の下部で編集できます。**

### 1. おすすめライドプラン（プランカード）
- TRBのツアーカード型。「豪徳寺・招き猫ループ」「多摩川1日ライド」「駒沢サンセット」の3コースを4言語で掲載
- ボタンを押すと予約ウィジェットにおすすめ車種・時間が自動セットされます
- 内容の変更は `OCTO_CONFIG.plans` を編集

### 2. トラストバー（クチコミ導線）
- Googleクチコミ / Tripadvisor / Instagram へのリンクボタン
- `OCTO_CONFIG.links` にURLを設定。空文字 `""` にするとそのボタンは非表示
- **Googleビジネスプロフィール登録後、正式なクチコミURLに差し替えてください**

### 3. 公式サイト特典バッジ（Book Direct訴求）
- 予約チケット内に「公式サイト予約がいちばんお得」と表示
- 文言は `OCTO_CONFIG.promo`、非表示にするには `features.promoBanner: false`

### 4. 安心サポート＋日本の自転車ルールガイド
- ヘルメット無料・プロ整備・4言語対応の3枚カード
- 外国人向け「左側通行・夜間ライト」などのルール早見表（4言語）

### 5. ホテル配達セクション（初期状態：非表示）
- 配達サービスを始めるときに `features.delivery: true` にすると表示されます

### 6. お客様の声セクション（初期状態：非表示）
- 実際のレビューが集まったら `OCTO_CONFIG.reviews` に追加すると表示されます
- ※実在しないレビューは掲載しないでください（景表法・プラットフォーム規約の観点）

### 7. スマホ用固定「予約」バー＋WhatsApp/LINEボタン
- スマホ表示で画面下に常時「いますぐ予約」ボタン（TRBのチャット導線の代替）
- `links.whatsapp` / `links.line` にURLを入れると連絡ボタンが出ます
  - WhatsAppは欧米・東南アジア圏の旅行者の定番です。`https://wa.me/81XXXXXXXXXX`（先頭の0を取る）

### 8. ヒーロー動画対応
- `images/hero.mp4` を置くと自動で動画ヒーローになります（TRB型）。無ければ従来の画像のまま
- 10〜20秒・10MB以下・音声なしを推奨

### 9. FAQ拡充
- 「子どもは乗れる？（身長150cm目安）」「日本の交通ルールが不安」を4言語＋構造化データに追加

### 10. お客様の声：サンプル表示モード
- 現在は `sample: true` 付きのサンプルレビュー3件を表示中（カード右上に「サンプル/SAMPLE」バッジが付きます）
- **実在しないレビューを本物として掲載するのはステマ規制（景表法）違反リスクがあるため、公開運用ではバッジ付きのまま、または実レビュー到着後に差し替えてください**
- 実レビューに差し替える手順：`js/config.js` の `OCTO_CONFIG.reviews` で `sample: true,` の行を削除し、name / flag / text を実際の内容に書き換え


---

## 在庫・空き状況管理のはじめかた（自社運用・無料）

このサイトは2段階の在庫管理に対応しています。

### モード1：簡易モード（今すぐ・設定不要）
- `js/config.js` の `inventory`（保有台数）だけで動きます。予約画面に「残り◯台」の目安と定休日ブロックが表示されます
- 満車の日ができたら `blackoutDates` に日付を追加 → その日は予約不可になります
- 台帳は紙やメモでOKの、開業直後向けの運用です

### モード2：スプレッドシート連携モード（推奨・約10分で設置）
Googleスプレッドシートが「予約台帳」になり、**リアルタイム残数表示＋予約の自動記録＋新規予約の通知メール**が動きます。

**設置手順：**
1. Googleドライブで新しいスプレッドシートを作成（名前は「OCTO予約台帳」など）
2. メニュー「拡張機能」→「Apps Script」を開く
3. `gas/booking-api.gs` の中身を全部コピーして貼り付け、保存
4. 上部の関数選択で `setup` を選んで「実行」→ 権限を許可（台帳シートが自動作成されます）
5. 「デプロイ」→「新しいデプロイ」→ 種類「ウェブアプリ」
   - 実行ユーザー：**自分**
   - アクセスできるユーザー：**全員**
6. 発行された URL（`https://script.google.com/macros/s/.../exec`）をコピー
7. `js/config.js` の `availabilityApi: ""` にそのURLを貼り付けてアップロード

**日々の運用（1予約あたり30秒）：**
- 予約が入る → 通知メールが届く＆台帳に「仮予約」で自動記録
- Stripeの決済完了メールと突き合わせ → 台帳の状態を「決済済み」に変更
- 当日貸出時に「貸出中」、返却で「返却済み」、キャンセルは「キャンセル」に変更（キャンセルにすると在庫が自動で戻ります）
- 1泊2日の予約は翌日分の在庫も自動で引かれます
- 増車・減車したら `booking-api.gs` と `js/config.js` の両方の inventory を更新

### Stripeの決済手段を増やす（Alipay・WeChat Pay等）
1. Stripeダッシュボード →「設定」→「決済手段」（Payment methods）
2. Alipay / WeChat Pay を「有効化」（審査が入る場合があります。数日で完了することが多いです）
3. 有効化されると **既存のPayment Linksにも自動で表示されます**（リンクの作り直し不要）
4. 韓国系（カカオペイ等）の対応可否は国・アカウントにより異なるため、同じ画面で利用可能一覧を確認してください
5. 決済画面の表示言語はお客様のブラウザに自動で合わせられます

### ジャーナル（ブログ）の書きかた
1. `blog/gotokuji-lucky-cat.html` をコピーして新しいファイル名で保存（例：`blog/tamagawa-sunset.html`）
2. ファイル冒頭のコメントに従って title / description / OGP / 本文を書き換え
3. `js/config.js` の `OCTO_CONFIG.posts` に1件追加（4言語のタイトルと抜粋）
4. `sitemap.xml` に記事URLを1件追加してアップロード
- 記事は英語メイン＋日本語要約の形式が、インバウンドSEOに最も効きます
- ネタ例：多摩川サンセットガイド／世田谷のコーヒースタンド巡り／日本の自転車ルール完全ガイド

### 11. デザインv2「Editorial Motion」（2026-08 更新）
- カードグリッドを廃止し、雑誌型の互い違いレイアウト（車両）・フルワイドバンド（プラン）・枠なしプルクォート（お客様の声）に刷新
- 動き：ヒーローのマスク付きテキスト登場、画像のカーテンリビール、スタッガー表示、パララックス、スクロールで隠れるヘッダー、FAQの＋回転
- prefers-reduced-motion（視差効果を減らす設定）の端末では自動でアニメーション停止
- 追加画像なしで動作。デザイン変更はCSSのみなので、以前のデザインに戻す場合は css/style.css を旧版に差し替えるだけ
